# MLX model — HuggingFace repo ID or local path
MLX_MODEL=mlx-community/Llama-3.2-3B-Instruct-4bit

# Corruption settings
CORRUPTION_SEED=42
CORRUPTION_RATE=0.01        # Fraction of weights to corrupt (0.0 - 1.0)
CORRUPTION_MAGNITUDE=1.0    # Scale of noise injected

# Output
RESULTS_DIR=results/
